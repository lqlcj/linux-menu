# Hysteria2 节点搭建说明（小白版）

> 本文基于本项目脚本 `leyili.sh`，从零开始教你在自己的 VPS 上搭一个 Hysteria2（简称 HY2）节点。
> 全程跟着复制粘贴就行，不需要提前懂任何术语。

---

## 一、HY2 是什么，能干嘛

- **Hysteria2** 是一种基于 QUIC（UDP）的代理协议，特点是**抗丢包强、跑得快**，特别适合丢包率高的线路（比如晚高峰的中美链路）。
- 跟 Reality（TCP）相比，HY2 在弱网下体验通常更顺滑。
- 同一台 VPS 上可以**同时**装 Reality 节点和 HY2 节点，两者互不冲突。

---

## 二、你需要准备什么

1. **一台 VPS**
   - 系统：**Debian 10+ / Ubuntu 20.04+**（脚本只支持 Debian 系）。
   - 权限：**能用 root 登录**，或者能 `sudo -i` 切到 root。
   - 你需要知道服务器的 **IP 地址**和 **SSH 端口**（默认 22）。

2. **一台能 SSH 的电脑**
   - Windows：用自带的 PowerShell 或下载 [Termius](https://termius.com/) / [WindTerm](https://github.com/kingToolbox/WindTerm)。
   - macOS / Linux：直接用终端。

3. **客户端代理软件**（HY2 节点装好后，你的电脑/手机上要装一个能用 HY2 的客户端，下面会列出推荐）。

---

## 三、第一次进入脚本

### 1. SSH 登录到服务器

打开终端，输入（把 `你的IP` 换成实际 IP）：

```bash
ssh root@你的IP
```

输入密码后进入服务器。

### 2. 一键运行脚本

复制这一整行粘贴到服务器终端，回车：

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/lqlcj/linux-menu/main/leyili.sh)
```

第一次运行会自动把脚本注册成 `sb` 命令。下一次只要输入 `sb` 就能直接打开菜单。

### 3. 看到主菜单

会出现类似这样的界面：

```
管理菜单
│  版本      ·  未安装
│  状态      ·  未安装
│  Reality    ·  未安装
│  Hysteria2  ·  未安装
─────────────────────────
  1) 管理员设置
  2) 系统基础设置
  3) 创建节点
  4) 查看状态
  5) 外部服务
  6) IPv6 防火墙管理
  7) 卸载脚本
  8) 更新脚本
  0) 退出
```

---

## 四、创建 HY2 节点（核心步骤）

### 第 1 步 进入创建菜单

在主菜单输入 `3`，回车。

如果是全新机器，直接看到：

```
1) 创建 Reality 节点
2) 创建 Hysteria2 节点
0) 返回上级
```

输入 `2`，回车。

### 第 2 步 选端口

```
端口 (41207, 回车随机):
```

- 括号里是脚本随机给你挑的一个高位 UDP 端口（每次不一样）。
- **小白建议：直接回车**，用这个随机端口。
- 如果你想自己指定，比如 `443`，也行，输入数字后回车。

### 第 3 步 给节点取个名字

```
节点名称 (hy2):
```

- 这个名字只是显示在客户端里的别名，比如 `JP-HY2`、`美西`、随便起。
- 直接回车就用默认 `hy2`。

### 第 4 步 选监听模式

```
监听模式：
  1) 仅 IPv4 入站 + 仅 IPv4 出站 - 0.0.0.0（默认）
  2) 双栈入站 + 仅 IPv4 出站 - ::
  3) 仅 IPv6 入站 + 仅 IPv4 出站
请选择 (1):
```

怎么选？

| 你的情况 | 选哪个 |
|---|---|
| 普通 VPS（既有 IPv4 又有 IPv6，但你只在乎 v4） | **1**（默认） |
| 想让客户端可以同时用 v4 和 v6 连进来 | **2** |
| 服务器**只有 IPv6 没有 IPv4**（廉价 IPv6-only 机） | **3** |

**犹豫的话直接回车选 1。**

### 第 5 步 选证书来源（关键，仔细看）

```
证书来源：
  1) 自签证书（推荐，客户端 insecure=1）（默认）
  2) ACME 自动签发（需要真实域名 + 80 端口可用）
请选择 (1):
```

#### 先理解：HY2 为什么需要证书

HY2 跑在 **TLS over QUIC** 上。每次客户端连进来，都要做一次 TLS 握手。握手时服务器要出示一张"证书"证明自己。这张证书在 HY2 里有两种来源——**自己签的**和**外部权威机构签的**。

#### 选项 1：自签证书（self-signed）

**怎么签的**：脚本在你的服务器上用 `openssl` 现场生成一对密钥和一张证书，**签发人就是你自己**。整个过程不联外网，不要域名，不要 80 端口。

| 项目 | 说明 |
|---|---|
| 是否需要域名 | **不需要** |
| 是否需要 80 端口 | **不需要** |
| 客户端需要做啥 | 必须开 `insecure=1`（也叫"跳过证书校验"/"allow insecure"），脚本生成的链接里已经带了 `insecure=1`，多数客户端会自动识别 |
| 证书过期 | 脚本签的有效期 10 年，几乎一辈子用 |
| 安全性 | 在代理场景下**完全够用**——客户端和服务端都是你自己的，密码 + obfs 已经保证了认证和保密。`insecure=1` 只是"不去找公共 CA 验证"，不是"不加密" |

**适合：99% 的小白，包括没有域名、用 IPv6-only 廉价机、嫌折腾的所有人。**

#### 选项 2：ACME（Let's Encrypt 自动签发）

**怎么签的**：sing-box 启动时去找 Let's Encrypt 这家免费 CA，按 HTTP-01 流程自动申请证书——CA 会通过 80 端口反向访问你的域名验证你确实拥有它，验证通过就发一张正规证书。证书 90 天到期，sing-box 会自动续。

| 项目 | 说明 |
|---|---|
| 是否需要域名 | **必须有**，且域名的 A/AAAA 记录已经指向本机 IP（`ping 域名` 能 ping 出本机 IP） |
| 是否需要 80 端口 | **必须空闲**，签发和续期都用 80 端口做 HTTP 验证。如果机器上跑着 nginx / 1Panel / Caddy 占用 80，就签不下来 |
| 客户端需要做啥 | 不需要 `insecure=1`，链接里也不会带 |
| 证书过期 | 90 天，sing-box 自动续，无人值守 |
| 适合 | 有自己域名、想要"端到端正规 TLS"、或者把 HY2 跟普通 HTTPS 服务一起部署的进阶用户 |

#### 一句话对比

| 维度 | 自签 | ACME |
|---|---|---|
| 需要花钱买什么吗 | 都不要 | 至少要个域名（约 $1–$10/年） |
| DNS 配置 | 不用 | 要把域名 A 记录指向 VPS |
| 80 端口 | 不用 | 必须空着 |
| 客户端配置 | 链接自带 `insecure=1`，多数客户端无感 | 干净链接，无 `insecure` |
| 翻墙效果 | **没区别** | **没区别** |

**结论：纯翻墙用途下两者代理速度、稳定性、隐蔽性完全一样。区别只在"要不要域名 / 要不要客户端跳过校验"。小白闭眼选 1。**

---

### 第 6 步 SNI / 伪装域名（HY2 的 SNI 跟 Reality 不是一回事！）

**这是最容易混淆的一步**。如果你之前装过 Reality，会习惯性以为 HY2 的 SNI 也得填一个高质量的真实大网站（比如 `www.ucla.edu` / `www.microsoft.com`）。**完全不是这样**。下面把两者掰开讲。

#### Reality 的 SNI（你装 Reality 时填的那个）

- 它是一个**真实存在、对外开放、流量大、TLS 1.3 + X25519 + h2 都齐全**的第三方网站，比如 `www.microsoft.com`、`www.ucla.edu`、`www.cloudflare.com`。
- Reality 协议会**真的把 TLS 握手转发**给那个网站，让 GFW 看到的是一个真实通往该网站的 TLS 流量。客户端拿到的证书也是那个网站的真证书。
- **必须挑得好**——挑了个不通的、不稳定的或者关掉 TLS 1.3 的，Reality 直接连不上。
- 这是 Reality 的核心抗封锁机制，叫"借壳"。

#### HY2 的 SNI（这一步要填的）

- HY2 **没有借壳机制**，它的 SNI 只是 TLS 握手里那个 ServerName 字段的字面值，**和外部任何网站没有关系**。
- 不会有任何流量真的发去 `bing.com`，HY2 服务端自己用自己的证书完成握手。
- 这个域名只在两个地方用到：
  1. 写进自签证书的 CN（Common Name）字段，仅作为标签。
  2. 客户端发起 TLS 握手时填进 ClientHello 的 SNI 字段，让外部观察者看到你"好像在访问 bing.com"。

#### 那填什么合适？

##### 自签模式（第 5 步选了 1）

**填什么都行，没有任何"必须是大网站"的硬性要求。** 但**建议**填一个常见、不显眼的域名：

- 默认值 `bing.com` 完全可用，回车即可。
- 也可以换成 `www.cloudflare.com`、`www.apple.com` 这些大众的。
- 不要写 `localhost`、`mybox.local`、`hy2.test` 这种明显不存在的——容易被特征识别。

**关键区别**：Reality 选错 SNI 直接连不上；HY2 自签模式下 SNI 怎么写都能连上，差别只在"被审查系统看到的域名长什么样"。

```
伪装 SNI (bing.com):
```

直接回车用 `bing.com` 即可。

##### ACME 模式（第 5 步选了 2）

```
真实域名（已解析到本机）:
ACME 邮箱:
```

- 第一行：**必须填你自己的真实域名**，且该域名的 A 记录（IPv4）或 AAAA 记录（IPv6）已经解析到本机 IP。Let's Encrypt 要靠这个验证你拥有该域名。比如 `hy2.example.com`。
- 第二行：你的邮箱，Let's Encrypt 用来发证书到期提醒。随便一个能收信的就行。

ACME 模式下你**没有"挑伪装域名"的自由**——它只能是你自己拥有的真实域名。

#### 总结对照表

| 项目 | Reality | HY2 自签 | HY2 ACME |
|---|---|---|---|
| SNI 是什么角色 | 真实借壳目标 | 自签证书的 CN + 客户端 SNI 标签 | 自己的真实域名 |
| 必须是真实存在的网站吗 | **必须**（且 TLS1.3+h2） | 不必须（建议常见域名） | 必须是**自己拥有**的域名 |
| 需要拥有该域名吗 | 不要 | 不要 | **必须** |
| 写错的后果 | 直接连不上 | 能连，但伪装效果差 | ACME 签发失败 |
| 默认推荐 | `www.ucla.edu`（脚本默认值） | `bing.com`（脚本默认值） | 用户自己填 |

---

### 第 7 步 选 obfs（混淆）

```
obfs 混淆 (salamander)：
  1) 启用，密码自动生成（默认）
  2) 不启用
请选择 (1):
```

#### obfs 是什么，解决什么问题

HY2 底层是 **QUIC over UDP**。原始 QUIC 协议有自己一套很特征化的头部和握手流程——`Initial` / `Handshake` / `1-RTT` 这些包，有固定的版本字段、连接 ID 长度、Long Header 标志位。这些特征在抓包工具里**一眼就能认出来**："噢，这是 QUIC"。

正常情况下"被识别为 QUIC"不算什么大事。但在某些网络环境里：

- **运营商 QoS**：有些 ISP 会主动给 QUIC 流量限速（因为 YouTube / Google 大量用 QUIC，他们想压一压）。
- **校园网 / 公司网 / 部分 GFW 节点**：直接屏蔽或干扰 QUIC 协议。
- **DPI 设备**：把 QUIC 流量打标签，方便后续策略处理。

只要你的流量"长得像 QUIC"，就可能被波及。

#### salamander 是干啥的

`salamander` 是 Hysteria 官方设计的一层**字节级混淆**：

- 在每个 UDP 数据包外面再套一层用密码加密的随机化字节流。
- 加密后的包**没有任何 QUIC 特征**——头部字节完全随机。
- 在网络中间设备（DPI、运营商盒子）看来：这就是一坨**普通的、看不出协议的 UDP 流量**，没法判定是 QUIC、是 WireGuard、还是别的什么。
- 客户端拿到包之后，用同一个密码解开外层混淆，里面才是真正的 QUIC 流量。

**简单说：obfs 让你的流量从"明显是 QUIC"变成"看不出是什么"。**

#### 开启 vs 不开启的代价

| 维度 | 开启 salamander（推荐） | 不开 |
|---|---|---|
| 抗 QUIC 识别 | 完全规避 | 一抓一个准 |
| 流量特征 | 高熵随机字节 | 标准 QUIC 特征 |
| CPU 开销 | 极小（每个包做一次 AEAD），**家用 VPS 跑满千兆都不会瓶颈** |  |
| 协议兼容性 | 客户端必须支持 obfs（主流客户端都支持） | 任意 HY2 客户端都能连 |
| 客户端配置 | 链接自带 `obfs=salamander&obfs-password=xxx`，扫码或粘贴链接后无感 | 无 |
| 启动速度 | 无差别 | 无差别 |

#### 密码哪来的，要记住吗

- 脚本会用 `openssl` 自动生成一个 22 位随机字符串（`obfs-password`）。
- **服务端和客户端必须用同一个 obfs 密码**，否则解不开外层包，连接直接失败。
- 你**不用记**——它会写进客户端链接的 `obfs-password=xxx` 参数里。导入链接 / 扫二维码时，客户端自动拿到。

#### 啥时候才考虑选 2（不开）

99% 的情况都开。极少数场景才不开：

- 用一个**老旧或者非主流的 HY2 客户端**，发现不支持 obfs（罕见）。
- 网络环境**完全不存在 QUIC 干扰**（你确认过，不是猜的）。
- 想做协议层面的实验，比对开关 obfs 的对照样本。

**结论：直接回车选 1，开 salamander。无脑开就对了。**


### 第 7.5 步 选带宽限制（**新加的步骤，强烈建议设置**）

```
带宽限制（防 HY2 跑满线路被云厂商盯，不影响日常使用）：
  1) 保守    上 30  / 下 80  Mbps  - 小机器 / 共享带宽限速重的
  2) 推荐    上 100 / 下 300 Mbps  - 99% 情况，闭眼选（默认）
  3) 大带宽  上 300 / 下 800 Mbps  - 测速过、确认有大带宽
  4) 自定义
  5) 不限制（不推荐，可能触发云厂商告警）
请选择 (2):
```

#### 为什么必须设置

HY2 默认开启 **Brutal 拥塞控制**——这玩意会无视 TCP 公平性，**强行抢占整个网卡带宽**。如果不限制，会出现：

1. **触发云厂商流量告警** — Vultr / DigitalOcean / Hetzner 等看到机器持续跑满会限速、警告甚至停机
2. **干扰邻居用户** — 大流量持续时同 IDC 的其他机器可能受影响，被投诉
3. **GFW 流量画像异常** — 普通用户不会 7×24 跑满，曲线像非自然流量 → 加重点关注
4. **触发 IDC 反 DDoS** — 某些 IDC 把持续大流量 UDP 当成攻击源

**限带宽不影响日常使用**——限的是单连接峰值，正常看视频（包括 4K）、下载、游戏都用不到这个上限。

#### 各档位怎么选

| VPS 实际能跑多少 | 建议档位 | 上行/下行 |
|---|---|---|
| 不知道 / 小机器（Lowend） | 1) 保守 | 30 / 80 Mbps |
| **绝大多数情况** | 2) 推荐 ◀ 默认 | 100 / 300 Mbps |
| 大流量场景（自己测过） | 3) 大带宽 | 300 / 800 Mbps |
| 想精确控制 | 4) 自定义 | 自己填 |
| 极少数情况（不推荐） | 5) 不限制 | 真的会跑满 |

#### 关于 VPS 标称带宽

- "10 Gbps 端口" 是**物理网卡**带宽，是**所有租户共享**的——你能跑多少看 IDC 当前负载和限速策略
- VPS 厂商常见的实际限速：100 Mbps 共享 ~ 1 Gbps 共享，10 Gbps 端口的真实可用值往往低于标称
- 限速档位是给你日用的"上限"，不是流量配额——**不会扣你流量包**

**犹豫的话：直接回车选 2（推荐档），完事。**


### 第 7.6 步 端口跳跃（**新加的进阶选项**）

```
端口跳跃 (port hopping) — 进阶选项：
  作用：客户端在多个端口间随机跳跃，封一个端口没用，对抗成本提升 100 倍
  代价：消耗大量端口，部分云厂商可能告警，可能与 Docker / 其他服务冲突
  1) 不启用（默认，推荐小白）
  2) 启用 - 自动选择范围（主端口 +1 到 +999）
  3) 启用 - 自定义范围（避开 Docker / 其他服务）
请选择 (1):
```

#### 这是什么，解决什么问题

正常情况下 HY2 节点只监听**一个 UDP 端口**（比如 41207）。如果 GFW 识别到这个端口跑代理流量，**精准封锁这一个端口**就能让你掉线。

端口跳跃让客户端在一段连续 UDP 端口（比如 41208-42206 共 999 个）里**随机切换**，服务端通过 iptables NAT 把整段范围 REDIRECT 到主端口。在 GFW 那边看：流量同时来自 1000 个不同端口——**封一个没用，得整段封**。

| 维度 | 不启用 | 启用 |
|---|---|---|
| 对抗成本（封锁方） | 封一个端口即可 | 必须封整段 ~1000 个端口 |
| 你这边的端口占用 | 1 个 | 1000 个（NAT 范围全占用） |
| 云厂商告警风险 | 低 | 较高（流量分散在大量端口可能被识别） |
| iptables 配置复杂度 | 无 | 全自动，重启自动恢复 |
| 客户端配置 | 直接连 | 链接里自带 `port,start-end` 范围语法，扫码即用 |

#### 自动 vs 自定义

**自动模式（选 2）**：
- 脚本算出主端口附近的 999 端口范围（优先放主端口上方，上方不够放下方）
- 比如主端口 41207 → 范围 41208-42206
- 适合纯净系统、没跑 Docker 或其他服务的机器

**自定义模式（选 3）**：
- **如果你的机器跑了 Docker 或其他服务**，强烈建议用这个
- 输入格式：`30000-31000`（含两端，共 1001 个端口）
- 脚本会校验：端口在 1024-65535 之间、起始 < 结束、不包含主端口、范围太小（<50）会警告
- 启用前还会扫描整个范围内是否有 UDP 服务在监听，有冲突会列出来让你确认

```
建议范围至少 100 个端口，越大抗封效果越好
格式示例：30000-31000  (含两端，共 1001 个端口)
建议避开：22 (SSH)、80/443 (Web)、Docker 已用端口

端口范围 (起始-结束):
```

#### 自动做了什么

启用端口跳跃后，脚本会**全自动**完成：

1. 创建独立 iptables 链 `LEYILI_HOP_NAT`，把范围内所有 UDP 端口 REDIRECT 到主端口
2. IPv6 / 双栈 / 仅 IPv6 模式自动添加对应的 `ip6tables` 规则
3. 监听具体 IPv6 地址时（v6-in-v4-out 模式）使用 DNAT 显式指定目标，避免多 v6 地址机器落错地址
4. 自动放行 ufw / firewalld 整段端口范围
5. 持久化到 `/etc/iptables/rules.v4` 和 `rules.v6`，**重启自动恢复**
6. 改主端口时自动重算范围、卸载节点时自动清理规则

**不需要你手动改 iptables、不需要重启后再加规则。**

#### 什么时候该开

- 已经被 GFW 重点关注的线路（封端口频繁）
- 高级用户、有动态对抗需求
- 不在意云厂商可能告警

#### 什么时候不要开

- 全新机器、节点用得很轻
- 对云厂商告警敏感（VPS 是正经业务用途，不能被关停）
- 不熟悉 iptables / NAT，担心冲突

**结论：小白直接选 1（不启用）。等被封端口被封过两三次，再回来开。**


### 第 8 步 等待安装完成

脚本会自动做这些事：

1. 装 sing-box（如果是首次）
2. 生成密码、密钥
3. 写配置文件
4. 启动服务
5. 给你打印**客户端链接 + 二维码**
6. 给你打印**端口放行提示**（**注意：脚本不会自动开防火墙，需要你手动开**）

---

## 五、关键：脚本不会自动开放端口，要自己开

### 为什么要自己开

脚本只在 sing-box 里把节点启起来了。**节点能不能从外网连进来，取决于服务器的防火墙是否放行了对应的端口和协议**。脚本故意不去自动改防火墙，原因是：

- 自动改防火墙容易和你已有的安全策略打架（特别是上了 1Panel / 自己写过 ufw / iptables 的环境）。
- 不同节点用的协议不一样（Reality 走 TCP，HY2 走 UDP），加错协议=连不上。
- 真正阻挡连接的常常不是机器本身的防火墙，而是**云厂商控制台里的"安装组 / 安全规则"**——这个脚本没法替你改。

所以策略是：**装完后脚本会清楚告诉你"放行什么端口、什么协议、用什么命令"，你按提示开就行。**

### 安装完会看到的端口提示（示例）

```
请自行放行入站端口：41207/udp  (Hysteria2 节点入站)
    · ufw    : ufw allow 41207/udp
    · IPv6 防火墙菜单 : 主菜单 6) IPv6 防火墙管理 → 4) 开放端口
    · 云厂商安全组    : 阿里云 / 腾讯云 / AWS / Vultr 等控制台需自行加 41207/udp 入站规则
```

如果你装的是 ACME 模式，会**多打印一条 80/tcp 的提示**——证书签发时 Let's Encrypt 要用 80 端口反向访问你的服务器，必须放行才能签下来。

### 三条放行通道（按你的环境选）

#### 通道 1：IPv4 防火墙（ufw 或 firewalld）

如果机器上启用了 ufw（Ubuntu 常见）：

```bash
ufw allow 41207/udp        # 把 41207 换成你的实际端口
ufw status                 # 看一眼有没有加上
```

如果是 firewalld（CentOS/RHEL/Rocky 常见）：

```bash
firewall-cmd --permanent --add-port=41207/udp
firewall-cmd --reload
firewall-cmd --list-ports
```

如果两个都没启用（脚本输出会提示 "本机未启用 ufw/firewalld"），说明你的机器**没有 IPv4 软件防火墙**——这种情况下默认是全开的，IPv4 那边什么都不用做。

#### 通道 2：IPv6 防火墙（脚本自带的菜单）

如果你之前用过本脚本的 `主菜单 6) IPv6 防火墙管理 → 一键初始化`，那么 IPv6 是默认拒绝所有入站的，**HY2 走 IPv6 就连不上**，必须手动放行：

```
sb                          # 进菜单
6                           # IPv6 防火墙管理
4                           # 开放端口
2                           # 选 UDP
41207                       # 端口号
```

注意菜单里的协议选择项：

| 选项 | 适合谁 |
|---|---|
| 1) TCP | Reality 节点 / 网站服务 |
| 2) UDP | **HY2 节点（必选）** |
| 3) TCP + UDP | 只有少数特殊用途才需要全开 |

**搞反协议=连不上**，HY2 一定选 2 (UDP)，Reality 选 1 (TCP)。

如果你**没用过 IPv6 防火墙菜单**，IPv6 是默认全开的，跳过这一步即可。

#### 通道 3：云厂商安全组（最常见的"为啥连不上"）

阿里云 / 腾讯云 / AWS / Azure / Vultr / DigitalOcean 等几乎所有 VPS 厂商的控制台里，都有一层**外置的安全组规则**，独立于机器本身的防火墙。**机器内部 ufw 全开了也没用，安全组挡住就是挡住。**

通用步骤：

1. 登录云厂商控制台，找到你的实例。
2. 找到 "安全组 / 防火墙规则 / Firewall / Security Group / Networking"。
3. 加一条入站规则：
   - 协议：**UDP**（HY2）或 **TCP**（Reality）
   - 端口：你的节点端口（比如 41207）
   - 源 IP：`0.0.0.0/0`（任何地方）和 `::/0`（IPv6 任何地方）
4. 保存生效（云厂商一般几秒到一分钟）。

### 怎么验证开成功了

在**自己电脑**（不是服务器！）上：

```bash
# Linux / macOS（要装 nmap）
nmap -sU -p 41207 你的IP                  # 测 UDP
nmap -p 41207 你的IP                       # 测 TCP

# 或者干脆直接用客户端连一下，最直接
```

如果 nmap 显示 `open` 或 `open|filtered`，UDP 就到位了（UDP 没有握手所以总是 filtered/open 难判断，最靠谱还是直接连）。

### 改了端口怎么办

通过菜单 `4) 查看状态 → 2) 修改节点参数` 改了端口后，脚本会再次打印新端口的放行提示。**记得**：

1. 先放新端口（参考上面三条通道）。
2. 老端口的旧规则可以留着不管，也可以反过来撤销（`ufw delete allow 老端口/udp` 或 `firewall-cmd --permanent --remove-port=老端口/udp`）。

---

## 六、安装完成后看到的东西

```
╔══════════════════════════════════════════════════════╗
║  Leyili  Hysteria2 节点创建完成                       ║
╚══════════════════════════════════════════════════════╝
  模式      : 仅 IPv4 入站 + 仅 IPv4 出站
  端口      : 41207 (UDP)
  入口 IP   : 1.2.3.4
  SNI       : bing.com
  证书      : self  (客户端需 insecure=1)
  Password  : aBcD1234EfGh5678IjKl
  Obfs      : salamander  ObfsPwd : Mn90PqRsTuVw...
  带宽限制  : 上 100 / 下 300 Mbps
  端口跳跃  : 41208-42206 (auto 模式, 999 端口)     ← 启用了才显示
  出站策略  : 仅 IPv4

  客户端链接：
  hysteria2://aBcD1234EfGh5678IjKl@1.2.3.4:41207,41208-42206?sni=bing.com&insecure=1&obfs=salamander&obfs-password=Mn90...#hy2
                                              ↑ 启用端口跳跃后链接里多了这段范围
  [二维码图形]

  信息已保存至 /etc/sing-box/nodes/hy2.info
```

**重点：把 `客户端链接：` 后面那一长串 `hysteria2://...` 复制下来**，这就是导入到客户端用的链接。

---

## 七、客户端怎么用

### Windows / macOS / Linux 桌面端

| 客户端 | 是否原生支持 HY2 | 备注 |
|---|---|---|
| **Mihomo Party**（原 Clash Verge Rev） | ✅ | Windows / macOS 通用，最推荐 |
| **NekoBox / NekoRay** | ✅ | 多协议都支持 |
| **sing-box 官方 GUI** | ✅ | 原生支持 |

### 安卓

| 客户端 | 备注 |
|---|---|
| **NekoBox for Android** | 推荐 |
| **sing-box for Android** | 官方 |
| **Hysteria** 官方 app | 可选 |

### iOS / iPadOS

| 客户端 | 备注 |
|---|---|
| **Shadowrocket**（小火箭） | 付费，支持 HY2 |
| **Stash** | 付费 |
| **sing-box** | 免费但要美区 ID |

### 导入方式（通用）

绝大多数客户端都支持下面任意一种：

1. **复制链接 → 客户端"从剪贴板导入"**
   - 把 `hysteria2://...` 整段链接复制到剪贴板，打开客户端找"添加节点 / 从剪贴板导入"。

2. **扫码导入**
   - 在脚本输出的二维码上用客户端扫一下。
   - 也可以在脚本菜单里：`4) 查看状态 → 8) 节点二维码`，随时调出。

---

## 八、装好之后能干啥（管理操作）

### 在主菜单输入 `4` 进入"查看状态"

```
1) 查看运行状态        # systemctl status
2) 修改节点参数        # 改端口/SNI/密码/带宽/端口跳跃 ← 被 Q 时来这里
3) 实时日志            # 看 sing-box 跑的日志，按 Ctrl+C 退出
4) 重启服务
5) 停止服务
6) 启动服务
7) 查看客户端链接      # 重新打印链接
8) 节点二维码          # 重新出二维码
9) 查看配置
10) 编辑配置（vim）
11) 清理配置备份
0) 返回
```

### 想改端口或密码

主菜单 → `4` → `2`，**多节点时会问你修改哪一个**，选 `hy2` 即可。

按提示一路改：

- 端口（回车保留）
- SNI（回车保留）
- 是否重新生成 Password（一般 N）
- 是否重新生成 obfs 密码（一般 N）
- 带宽限制（回车保留当前设置）
- 端口跳跃（回车保留当前设置）

改完会自动校验、重启服务、打印新链接。

### 想看节点信息

主菜单 → `4` → `6`，选 `hy2`。

### 想再加一个 Reality 节点

主菜单 → `3` → `1) 创建 Reality 节点`。两个节点共存，互不影响。

### 想只删 HY2 节点（保留 Reality）

主菜单 → `3` → `2) 卸载单个节点` → 选 HY2。

### 想把整个脚本和所有配置都清掉

主菜单 → `7) 卸载脚本`。会清节点、防火墙、配置、TCP 优化等一切，恢复成裸机状态。

---

## 九、常见问题

### Q1：连接不上 / 客户端转圈

按下面顺序排查：

1. **客户端有没有勾选 `跳过证书校验` / `insecure`？**
   自签证书必须勾。链接里的 `insecure=1` 部分，多数客户端会自动识别，但有的客户端要手动开关。

2. **三层防火墙都放行了吗？**（**本脚本不再自动放行节点端口**，详见第五节）
   - **本机服务起来没**：`ss -ulnp | grep <端口>`，看到 sing-box 在监听才算本机 OK。
   - **本机 IPv4 防火墙**：`ufw status` 或 `firewall-cmd --list-ports`，看放没放 `<端口>/udp`。没启用这俩就跳过。
   - **本机 IPv6 防火墙**：如果你之前一键初始化过本脚本的 IPv6 防火墙，必须到 `主菜单 6 → 4) 开放端口 → 选 UDP` 加放行，否则 IPv6 那条路被默认 DROP 拦住。
   - **云厂商安全组**：阿里云/腾讯云/AWS/Vultr 等控制台里加一条 `UDP <端口>` 入站。**脚本完全没法改云控制台**，这层最容易忽视。

3. **服务在跑吗？**
   主菜单 → `4) 查看状态` → `1) 查看运行状态`，应该是 `active (running)`。

4. **看日志有没有报错**
   主菜单 → `4) 查看状态` → `3) 实时日志`，复制报错关键字搜。

### Q2：ACME 模式提示证书签发失败

- 域名 A 记录是不是真的指向本机 IP？`ping 你的域名` 看看。
- 80 端口被占用了吗？`ss -tlnp | grep ':80'`，被 nginx / 1panel / Caddy 占着的话就签不下来。
- 实在不行，回去**重装节点改成自签模式**最省心。

### Q3：UDP 在某些网络环境被 QoS 限速

- 这是 HY2 的天生短板。如果发现某些场景下 HY2 反而比 Reality 慢，那条线路就是 UDP 不友好，**那种场景下用 Reality**。
- 这也是为什么本脚本支持两种节点共存——按线路情况切。

### Q4：随机端口每次都不一样能记住吗？

不需要记。链接 / 节点信息文件会保存。脚本主菜单顶部就显示着端口：

```
Hysteria2  ·  已安装  端口 41207  IP 1.2.3.4  +hop      ← +hop 表示端口跳跃已启用
```

随时 `sb` 进来都能看到。

### Q5：升级 sing-box 内核

主菜单 → `3) 节点 / 内核管理` → `3) 升级 sing-box 内核`。
不会动你的节点配置，只升级二进制。

### Q6：脚本本身要更新

主菜单 → `8) 更新脚本`。

### Q7：带宽限制了之后客户端实际还能跑多快？

限制的是**单连接峰值速率**，不是平均速率，也不是流量配额。

- 选"推荐档"（上 100 / 下 300 Mbps），看 4K 视频、下载、玩游戏完全够
- 想跑 iperf3 极限测速会被限到这个值，但日常使用感觉不到差异
- **限带宽不会扣你的流量包**，你的 VPS 月流量该多少还是多少

为什么要限：见第 7.5 步说明。一句话——防止 HY2 把网卡跑满被云厂商盯上。

### Q8：启用端口跳跃后，客户端要怎么填地址？

**完全不用手动配**。脚本生成的链接里已经带了范围语法：

```
hysteria2://密码@1.2.3.4:41207,41208-42206?sni=...&obfs=...
                       ↑      ↑
                       主端口  跳跃范围
```

- mihomo / sing-box / NekoBox 等主流客户端：扫码或粘贴链接后**自动识别**整个范围
- 客户端会在范围内随机选端口连接，自动跳跃
- 老版本 Shadowrocket 可能不支持范围语法，建议升级到最新版

### Q9：换主端口后端口跳跃会失效吗？

**不会**，脚本自动处理：

- **自动模式**：主端口变了，脚本基于新主端口重新计算范围（比如 41207 → 50000，范围从 41208-42206 变成 50001-50999）
- **自定义模式**：如果你的自定义范围不再包含新主端口，保持不变；如果新主端口意外落进了原范围，脚本会提示你重新输入新范围
- 整个过程：先清旧 iptables 规则 → 改主端口 → 加新 iptables 规则 → 重启服务 → 持久化。**重启 VPS 后规则也会自动恢复**。

修改入口：主菜单 → `4) 查看状态` → `2) 修改节点参数` → 选 hy2 → 跟着提示走。

### Q10：端口跳跃和 Docker 端口冲突了怎么办？

启用时选 **3) 启用 - 自定义范围**，自己输入一段**避开 Docker 已用端口**的范围。

举例：你 Docker 容器用了 30000-30100 区间的若干 host 端口，那就把跳跃范围设成 `40000-41000`，避开冲突。

如果不小心选了冲突范围，脚本会列出冲突的端口让你确认是否覆盖：

```
端口范围 40000-40500 内已有其他 UDP 服务监听：
  · 端口 40080 → users:(("nc",pid=1234,fd=3))
  仍然继续？冲突端口可能被 NAT 规则覆盖 (y/N):
```

---

## 十、文件清单（出问题时方便定位）

| 路径 | 说明 |
|---|---|
| `/etc/sing-box/config.json` | sing-box 主配置（多 inbound 共存） |
| `/etc/sing-box/nodes/hy2.info` | HY2 节点的所有参数（密码、端口、SNI、带宽限制、端口跳跃范围等） |
| `/etc/sing-box/nodes/reality.info` | Reality 节点的参数（如果装了） |
| `/etc/sing-box/certs/hy2.crt` | 自签证书 |
| `/etc/sing-box/certs/hy2.key` | 自签私钥 |
| `/etc/iptables/rules.v4` | 端口跳跃 iptables NAT/INPUT 持久化规则（v4） |
| `/etc/iptables/rules.v6` | 端口跳跃 ip6tables NAT/INPUT 持久化规则（v6） |
| `/usr/local/bin/sb` | 脚本快捷命令本体 |

**iptables 链名**：脚本创建的端口跳跃 NAT 链叫 `LEYILI_HOP_NAT`，可用 `iptables -t nat -L LEYILI_HOP_NAT -n -v` 查看（v6 用 `ip6tables -t nat -L LEYILI_HOP_NAT -n -v`）。卸载节点或卸载脚本时会自动清理。

---

## 十一、链式代理（中转机 → 落地机）

如果你有两台 VPS，想让 A 机器（中转机）的入站流量再转一层到 B 机器（落地机）出网，本脚本支持一键配置。典型用法：

- 你家附近 / 国内可达的"中转机"用 HY2（QUIC 抗丢包），客户端连这台
- 远端"落地机"用 Reality（可借壳大网站，海外侧低调），中转机的流量通过它出网

### 落地机（B）：什么都不用特殊做

落地机就是普通节点，**像你装单机节点一样**装一个 Reality 或 HY2 即可。装完后：

- 主菜单 → `4) 查看状态` → `6) 查看客户端链接`
- 复制 `vless://...reality...` 或 `hysteria2://...` 整段链接（这就是给中转机用的"通行证"）

### 中转机（A）：粘贴链接即可

1. 主菜单 → `3) 节点 / 内核管理` → `4) 链式代理设置 (中转机)`
2. 选 `1) 配置中转`
3. 选你想中转的入站（Reality 还是 HY2）
4. 把刚才从 B 机器复制的链接粘贴进去回车

脚本会自动：
- 解析链接（vless+reality / hysteria2 都支持）
- 在 sing-box 配置里加一条 chain outbound 指向 B
- 加一条路由规则：A 机器这个入站的流量 → 走 chain → 出到 B
- 校验 + 重启服务

完成后客户端连 A 机器看到的出口 IP 就是 **B 机器的 IP**。

### 验证

在 A 机器装好客户端的设备上访问 `https://api.ipify.org`，看到的应该是 **B 的 IP**，不是 A 的。

### 取消中转

主菜单 → `3) 节点 / 内核管理` → `4) 链式代理设置` → `2) 取消中转` → 选入站。

恢复直连。配置和证书不动，只是把 chain outbound + 路由规则清掉。

### 多入站独立中转

A 机器同时装了 Reality + HY2，可以**只让 HY2 走中转，Reality 直连**（或反过来）。在 `4) 链式代理设置` 里两个入站独立选，互不影响。

### 主菜单顶部摘要

配了中转的节点会在摘要里显示："`Hysteria2 · 已安装 端口 41207 IP 1.2.3.4 ↳中转→5.6.7.8:8443`"。

### 注意

- 落地机的端口（链接里 `:port` 部分）需要从中转机能 TCP/UDP 直连可达，**这层不在脚本控制范围**——云厂商安全组、运营商封禁等问题要自己保证。
- 中转机入站协议和落地机入站协议**可以不同**：A 跑 HY2 入站，B 跑 Reality 入站，中间这跳是 Reality 出站。
- 如果不小心把 A 机器自己的链接配给了 A 自己的中转（同一台机），会形成路由回环，脚本会粗略检查 IP 给警告但允许继续——**别这么干**。

---

## 十二、一句话总结

```
ssh 进服务器 → 跑脚本 → 主菜单输 3 → 输 2 → 一路回车 → 复制链接到客户端 → 完事
```

如果连这个流程都觉得长，那就**全程回车**就行，所有默认值都是为小白调好的。
