● 主菜单更新完成。做语法校验：

● Bash(bash -n "C:\Users\leyili\Desktop\linux-menu\linux-menu\leyili.sh" && echo "OK: syntax
      clean")
  ⎿  OK: syntax clean

● 语法检查通过。集成完成总结：

  改动点（仅 3 处编辑，原有功能未动）：

  1. 辅助函数（紧跟 allow_tcp_port_in_firewall 之后）

    - ip6_get_input_policy — 读 INPUT 默认策略
    - ip6_detect_ssh_port — 优先 ss 探测、回退 sshd_config
    - ip6_check_current_ssh_v6 — 判断当前 SSH 来源是否走 v6
    - ip6_ensure_persistence — 自动安装 iptables-persistent，且禁掉 v4/v6 自动保存（不会覆盖
  1Panel 的 v4 规则）
    - ip6_save_rules — 写入 /etc/iptables/rules.v6（Debian 系）
  2. 菜单页面（紧跟 show_external_services_menu 之后）

    - show_ipv6_firewall_menu 父菜单 + 7 个子功能：查看规则 / 查看监听 / 一键初始化 / 开放端口 /
   关闭端口 / 紧急放行 / 手动保存
    - 顶部状态行显示 INPUT 策略 + 自动检测的 SSH 端口
  3. 主菜单

    - 新增第 6 项「IPv6 防火墙管理」
    - 「卸载 sing-box」→ 7、「更新脚本」→ 8

  未触碰的部分：原 sing-box 安装的 3 种监听模式、所有原有功能。

---
  顺便一提，你最早问的「sing-box 安装的 3
  个监听模式不完善」我还没正式审过——要不要现在让我审一下？我快速看了一眼 do_install 的 2371-2411
   行，初步印象有几点值得讨论：

  - 模式 3 名字有歧义：「IPv6 入站 + IPv4 出站」实际 listen 用的是 ::，意味着 v4/v6 双栈入站 +
  强制 v4 出站，并不是「仅 v6 入站」
  - 缺「仅 IPv6 入站」模式：要做真正的 v6-only inbound，需要 listen 填具体 v6 地址，或用 [::] +
  关闭 bindv6only 的反向操作
  - dualstack 模式没有任何特殊出站处理：和 ipv4 模式相比只差 listen 地址，可能不是你想要的

  要继续审就告诉我，先放一放也行。

✻ Churned for 4m 4s

❯ 对你审核下，我记得singbox有个设置，默认出站可以优先IPV4还是只IPV4来自，我可能需要用到这个功能

● 来审。先把 3 个模式的实际行为和缺陷摆清楚，再讲你说的「优先/仅 IPv4 出站」该怎么补。

  当前 3 个模式实际行为

  ┌────────────┬─────────┬─────────────────────┬────────────────────┬─────────────────────┐
  │    模式    │ listen  │      出站配置       │      入站实际      │      出站实际       │
  ├────────────┼─────────┼─────────────────────┼────────────────────┼─────────────────────┤
  │ 1 仅 IPv4  │ 0.0.0.0 │ {"type":"direct"}   │ 只接 v4 ✅         │ 系统默认（v4/v6     │
  │            │         │                     │                    │ 都可能走）⚠️        │
  ├────────────┼─────────┼─────────────────────┼────────────────────┼─────────────────────┤
  │ 2          │ ::      │ {"type":"direct"}   │ v4+v6 双栈 ✅      │ 系统默认 ⚠️         │
  │ IPv4+IPv6  │         │                     │                    │                     │
  ├────────────┼─────────┼─────────────────────┼────────────────────┼─────────────────────┤
  │ 3 IPv6     │         │                     │                    │ 强制绑 v4           │
  │ 入站 +     │ ::      │ inet4_bind_address: │ v4+v6              │ 源地址，但 DNS      │
  │ IPv4 出站  │         │  <v4>               │ 双栈（名字误导）❌ │ 可能解到 AAAA →     │
  │            │         │                     │                    │ 失败 ⚠️             │
  └────────────┴─────────┴─────────────────────┴────────────────────┴─────────────────────┘

  三个核心问题

  1. 模式 3 名字与实现不符
  listen: "::" 意味着 v4/v6 都收，不是「仅 v6 入站」。要真正 v6-only inbound，得填具体 v6
    地址或用 inet6_bind_address。

  2. 出站 IP 协议族控制完全缺失（你问的就是这个）
  sing-box 的 direct outbound 有个关键字段 domain_strategy，可选值：

  ┌─────────────┬─────────────────────────────────┐
  │     值      │              行为               │
  ├─────────────┼─────────────────────────────────┤
  │ prefer_ipv4 │ 解析 A 和 AAAA，优先用 A        │
  ├─────────────┼─────────────────────────────────┤
  │ prefer_ipv6 │ 解析 A 和 AAAA，优先用 AAAA     │
  ├─────────────┼─────────────────────────────────┤
  │ ipv4_only   │ 只解析 A，目标无 A 记录直接失败 │
  ├─────────────┼─────────────────────────────────┤
  │ ipv6_only   │ 只解析 AAAA                     │
  └─────────────┴─────────────────────────────────┘

  当前脚本 3 个模式都没设这个字段，所以：
  - 模式 1 的「仅 v4」其实只控制了入站，出站照样可能走 v6
  - 模式 3 用 inet4_bind_address 强制 v4 socket，但 DNS 没限制——访问 youtube.com
  这类双栈站，sing-box 拿到 AAAA 后尝试 v6 连接，因为 socket 绑了 v4 直接连不通

  3. 模式 3 还有一个隐藏 bug
  route.final = "v4-out" 配了，但只有一个 outbound，没有兜底。如果 inet4_bind_address 那个 IP
    失效（比如重启后 IP 变了），整条链路死。

  建议的字段补齐

  每种模式的出站建议这样配：

  // 模式 1（仅 IPv4）
  "outbounds": [{
    "type": "direct",
    "domain_strategy": "ipv4_only"   // ← 新增
  }]

  // 模式 2（双栈）
  "outbounds": [{
    "type": "direct",
    "domain_strategy": "prefer_ipv4" // ← 新增，给个默认偏好
  }]

  // 模式 3（v6 入 + v4 出）
  "outbounds": [{
    "type": "direct",
    "tag": "v4-out",
    "inet4_bind_address": "<v4>",
    "domain_strategy": "ipv4_only"   // ← 关键，没这个就会断流
  }]

  还可以考虑加第 4 个模式

  既然你提到了「优先 v4 / 仅 v4」，菜单可以拆得更细：

  ┌────────┬────────────┬────────────────────────────┐
  │ 新模式 │    入站    │            出站            │
  ├────────┼────────────┼────────────────────────────┤
  │ A      │ 仅 v4 入站 │ 仅 v4 出站                 │
  ├────────┼────────────┼────────────────────────────┤
  │ B      │ 双栈入站   │ 优先 v4，回退 v6           │
  ├────────┼────────────┼────────────────────────────┤
  │ C      │ 双栈入站   │ 仅 v4 出站（绑 v4 源地址） │
  ├────────┼────────────┼────────────────────────────┤
  │ D      │ 双栈入站   │ 优先 v6（高级用户）        │
  └────────┴────────────┴────────────────────────────┘

  或者保留 3 个模式，每个加一个二级问题：「出站偏好？1) 仅 v4  2) 优先 v4  3) 跟随系统」。

  我的建议落地顺序

  1. 先小改：给 3 个模式各加 domain_strategy，模式 1=ipv4_only、模式 2=prefer_ipv4、模式
  3=ipv4_only。改完模式 3 才真正「仅 v4 出站」。
  2. 修模式 3 名字：菜单文案改成「双栈入站 + 仅 IPv4 出站」更准确。
  3. 可选加模式 4：如果你确实需要「双栈入站 + 优先 v4 出站（v4 不通时回退 v6）」这种弱限制场景。

  要不要我现在按第 1 步动手改？只动 do_install，三处加一个字段，几行代码，不破坏现有节点。

✻ Churned for 41s

❯ 可以，修复这些问题，都设置为IPV4出站吧
  ⎿  API Error: 503 Service Unavailable. This is a server-side issue, usually temporary — try
     again in a moment. If it persists, check status.claude.com.

✻ Cooked for 4m 11s